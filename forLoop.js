/* var roast = 0;

while (roast <= 20) {
    console.log(roast);
    roast++
} */

//  for frist time look

/* for (var roast = 0; roast <= 20; roast++) {
    console.log(roast);
} */


    // for actually look this 

/* for (var r = 0; r <= 12; r++) {
    console.log(r);
} */


// even number 

/* for (var r = 0; r <= 20; r+=2) {
    console.log(r);
}
 */

// odd number 

for (var r = 1; r <= 20; r+=2) {
    console.log(r);
}
